# Librarian discovery snapshot
Generated by developer/scripts/librarian-discovery.sh
Timestamp: 2026-01-28T05:37:00Z
Workspace: /Users/bsmith/bnjam/hyperion

## Anchor summary (limit 5, min count 2)
Top 5 files by anchor count:

.system/skill-installer/scripts/install-skill-from-github.py (22 anchors)
--------------------------------------------------------------------------
    22: Args (class)
       class Args:
    33: Source (class)
       class Source:
    41: InstallError (class)
       class InstallError(Exception):

doctor/scripts/include/doctor_parse.py (17 anchors)
----------------------------------------------------
    23: now_utc (def)
       def now_utc() -> datetime:
    28: to_rfc3339 (def)
       def to_rfc3339(dt: datetime) -> str:
    35: hash_content (def)
       def hash_content(content: str) -> str:

doctor/scripts/include/doctor_cli.py (12 anchors)
--------------------------------------------------
    25: cmd_status (def)
       def cmd_status(args: argparse.Namespace) -> int:
    45: cmd_init (def)
       def cmd_init(args: argparse.Namespace) -> int:
    63: cmd_surface (def)
       def cmd_surface(args: argparse.Namespace) -> int:

grape/scripts/include/grape_cli.py (11 anchors)
------------------------------------------------
    13: repo_root_from_file (def)
       def repo_root_from_file(file_path: Path) -> Path:
    24: cmd_help (def)
       def cmd_help(_: argparse.Namespace) -> int:
    31: cmd_validate (def)
       def cmd_validate(_: argparse.Namespace) -> int:

parallel-patcher/scripts/apply_patch.py (10 anchors)
-----------------------------------------------------
    16: _sha256_text (def)
       def _sha256_text(text: str) -> str:
    20: _load_json (def)
       def _load_json(path: Path) -> Dict[str, Any]:
    25: _emit_errors (def)
       def _emit_errors(errors: List[str]) -> int:

## Anchor chunk preview (chunk size 4, max 2)
Chunk 1 — .system/skill-creator/license.txt (1 anchors)
------------------------------------------------------------
   186: name (class)
       file or class name and description of purpose be included on the

Chunk 2 — .system/skill-creator/scripts/init_skill.py (4 anchors)
------------------------------------------------------------
   122: main (def)
       def main():
   194: normalize_skill_name (def)
       def normalize_skill_name(skill_name):
   203: title_case_skill_name (def)
       def title_case_skill_name(skill_name):
   208: parse_resources (def)
       def parse_resources(raw_resources):


## Anchors for plan file (WORKPLAN.md)
No anchors recorded for the plan file.
